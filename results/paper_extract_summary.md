PDF=C:\Users\28681\Desktop\Kalman\sematic\Chen 等 - 2024 - Structure-enhanced DRL for optimal transmission scheduling.pdf
PAGES=15 LEN=79664

=== Table I ===
 landLis the total number of layers of the NN [39]. Based on our computing platform, the inference time of the SE-DQN and SE-DDPG are 0.02 ms and 0.04 ms, respectively. The settings of the other hyperparameters for Algorithm 1 and Algorithm 2are summarized in Table I. Note that the input dimensions of the actor NN and the critic NN are different, as the inputs of the critic NN and the actor NN are the state vector and the state-action pair, respectively. During the training, we use the ADAM optimizer for calcu- lating the gradient and reset the environment for each episode with T= 500 steps. The episode numbers for the loose SE action, the tight SE action, and the conventional D

=== Algorithm 1 ===
d by the SE strategy and the greedy strategy: ADiQ(si,ai;)Q(si,ai;). (27) Since the optimal policy has the threshold structure, the inferred action ashould be identical to the optimal action a. Thus, the DRL algorithm should minimize the AD error (27).Algorithm 1 SE-DQN for Sensor Scheduling in the Remote Estimation System 1:Initialize replay memory Dto capacity N 2:Initialize policy network with random weights  3:Initialize target network with weights = 4:forepisode = 1,2, . . . , E 1do 5: Initialize state s0 6: fort= 1,2, . . . , T do 7: Generate atand select action atby the loose SE action selection method of SE-DQN 8: Execute action atand observe rtandst+1 9: Comput

=== Algorithm 2 ===
egy viand the actor NN vi, when viis executed, i.e., vi=vi. If the SE action is not selected, then the loss function is the Q-value given the state-action pair, which is identical to the conventional DDPG. Given the hyperparameter 2, the loss function forAlgorithm 2 SE-DDPG for Sensor Scheduling in the Remote Estimation System 1:Initialize replay memory Dto capacity N 2:Initialize actor network and critic network with random weights and 3:Initialize target network and with weight =,= 4:forepisode = 1,2, . . . , E 1do 5: Initialize a random noise Nfor action exploration 6: Initialize state s0 7: fort= 1,2, . . . , T do 8: Generate vtand select action vtby the loose SE a

=== SE-DQN ===
 work.SE action selection method based on Definitions 1and2to select reasonable actions and hence enhance the data sampling efficiency; and 2) an SE loss function definition to add the penalty to sampled actions that do not follow the threshold structure. Our SE-DQN training algorithm has three stages: 1) the DQN with loose SE action selection stage, which only utilizes part of the structural property, 2) the DQN with tight SE action selection stage utilizes the full structural property, and 3) the conventional DQN stage. The first two stages use the SE action selection schemes and the SE loss function to train the DQN fast, resulting in a reasonable threshold policy, and the l

=== SE-DDPG ===
f the actor NN, named as the virtual action v. It directly follows that the virtual action vand the real scheduling action acan be mapped from one to the other directly. Therefore, we use the virtual action v, instead of the real action a, when presenting the SE-DDPG algorithm. Similar to the SE-DQN, the SE-DDPG has the loose SE- DDPG stage, the tight SE-DDPG stage, and the conventional DDPG stage. The ith sampled transition is denoted as Ti(si,vi,vi,vt, ri,si+1). We present the SE action selection method and the SE loss function in the sequel. 1) SE Action Selection: The general action selection approach for the SE-DDPG is identical to that of the SE-DQN, by simply convertin

=== Loose SE ===
o select reasonable actions and hence enhance the data sampling efficiency; and 2) an SE loss function definition to add the penalty to sampled actions that do not follow the threshold structure. Our SE-DQN training algorithm has three stages: 1) the DQN with loose SE action selection stage, which only utilizes part of the structural property, 2) the DQN with tight SE action selection stage utilizes the full structural property, and 3) the conventional DQN stage. The first two stages use the SE action selection schemes and the SE loss function to train the DQN fast, resulting in a reasonable threshold policy, and the last stage is for further policy exploration. In what follows, 

=== Tight SE ===
 definition to add the penalty to sampled actions that do not follow the threshold structure. Our SE-DQN training algorithm has three stages: 1) the DQN with loose SE action selection stage, which only utilizes part of the structural property, 2) the DQN with tight SE action selection stage utilizes the full structural property, and 3) the conventional DQN stage. The first two stages use the SE action selection schemes and the SE loss function to train the DQN fast, resulting in a reasonable threshold policy, and the last stage is for further policy exploration. In what follows, we present the loose and tight SE action selection schemes and the SE loss function. 1) Loose SE Actio

=== conventional DQN ===
of different state-action pairs. To train the DQN, one needs to sample data (consisting of states, actions, rewards, and next states), define a loss function of based on the collected data, and minimize the loss function to find the optimized . However, the conventional DQN train- ing method has never utilized structures of optimal policies before. To utilize the knowledge of the threshold policy structure for enhancing the DQN training performance, we propose 1) an 8Note that the DRL training is conducted offline, which requires knowledge of the state-transition rules in Section III-A, building on the channel statistics. Online fine-tuning techniques for model mismatching issues durin

=== conventional DDPG ===
rget network: + (1) + (1) 18: end for 19:end for 20:forepisode E1= 1,2, . . . , E 2do 21: Repeat algorithm from line 5 to line 18 by adopting the tight SE action selection method 22:end for 23:forepisode E2= 1,2, . . . , E do 24: Execute the conventional DDPG algorithm as in [37] 25:end for the transitionTiis defined as Li() =( 2Q(si,vi;) + (12) (vivi)2,ifvi=vi Q(s i,vi;), otherwise, (31) and hence the overall loss function given the sampled batch is L() =1 BBX i=1Li(). (32) By replacing (30) and (31) in (32) and applying the chain rule, we can derive the gradient of the overall loss function in terms ofas L() =1 BBX i=1Li(), (33) whereLi()is giv

=== Section VI ===
resented in Section III. The proofs of structural properties of the optimal scheduling policy are given in Section IV. The structure-enhanced DRL algorithms for the formulated problem are presented in Section V. The numerical results are shown and analyzed in Section VI, followed by conclusions in Section VII. Notations: Matrices and vectors are denoted by capital and lowercase upright bold letters, e.g., Aanda, respectively. E[] is the expectation operator. ()is the vector or matrix trans- pose operator. ()1is the matrix inverse operator. Tr() is the matrix trace operator. Pr() andPr(|) denote the probability and conditional probability, respectively. NandRdenote the sets

=== N = ===
calculate the corresponding action a(n,m)(a(n,m) 1, . . . , a(n,m) n, . . . , a(n,m) N ) = arg max aQ(s(n,m),a;). From the channel-state and the AoI-state threshold properties, the scheduling action a(n,m) n should be identical to m. Thus, ifa(n,m) n =m, then the SE action for sensor nisan=m; otherwise, anis identical to the conventional DQN action. The SE action satisfying both the threshold properties is a= (a1,a2, . . . , aN). If such an action meets the constraint (5), then it is executed as a=a; otherwise, we select the greedy action a=a. 3) SE Loss Function: During the training, each transition (st,at,at,at, rt,st+1)is stored in a replay memory, wh

=== M = ===
ow. For notation simplicity, we use m > 0to denote the SE channel selection for sensor n. Given the state s, we define the state with a smaller channel ms state for sensor nas s(n,m)= ( ,H(n,m)), where H(n,m)andHare identical except the element h(n,m) n,m =hn,m1. Then, we calculate the corresponding action a(n,m)(a(n,m) 1, . . . , a(n,m) n, . . . , a(n,m) N ) = arg max aQ(s(n,m),a;). From the channel-state and the AoI-state threshold properties, the scheduling action a(n,m) n should be identical to m. Thus, ifa(n,m) n =m, then the SE action for sensor nisan=m; otherwise, anis identical to the conventional DQN action. The SE action satisfying both the thre

=== batch size ===
D2 i, otherwise, where 1is a hyperparameter to balance the importance of TDiandADi. In other words, if the SE action is executed, both the TD and AD errors are taken into account; otherwise, the conventional TD-error-based loss function is adopted. Given the batch size B, the overall loss function is L() =1 BBX i=1Li(). (28) To optimize , we adopt the well-known gradient descent method and calculate the gradient as below L() =1 BBX i=1Li(), (29) whereLi()is given as Li() =  2((11)AD i(Q(si,ai;)Q(si,ai;)) +1TDiQ(si,ai;)), ifai=ai 2(TD iQ(si,ai;)), otherwise. The details of the SE-DQN algorithm are given in Algorithm 1. B. Structure-Enhanced DD

=== episode ===
algorithm should minimize the AD error (27).Algorithm 1 SE-DQN for Sensor Scheduling in the Remote Estimation System 1:Initialize replay memory Dto capacity N 2:Initialize policy network with random weights  3:Initialize target network with weights = 4:forepisode = 1,2, . . . , E 1do 5: Initialize state s0 6: fort= 1,2, . . . , T do 7: Generate atand select action atby the loose SE action selection method of SE-DQN 8: Execute action atand observe rtandst+1 9: Compute action at= arg maxaQ(st,a;) 10: Store transition (st,at,at,at, rt,st+1)inD 11: Sample a random batch of transitions (si,ai,ai,ai, ri,si+1) 12: Calculate TDiandADibased on (26) and (27) 13: Update accordi

=== AoI ===
 Downloaded on February 03,2026 at 06:15:31 UTC from IEEE Xplore.  Restrictions apply.  380 IEEE TRANSACTIONS ON WIRELESS COMMUNICATIONS, VOL. 23, NO. 1, JANUARY 2024 in [3], [9], and [10] were derived as functions of individ- ual devices age of information (AoI) states. Thus, AoI is an important metric for characterizing cyber-physical system performance during dynamic scheduling and has attracted lots of attention in the communications society [12]. However, the conventional model-based solutions used in these works are not feasible in large-scale scheduling problems because of the curse of dimensionality caused by high-dimensional state and action spaces.1 In recent year

=== random ===
ment of the sensor n,AnRlnlnand CnRenlnare the system matrix and the measurement matrix, respectively, wn,tRlnandvn,tRenare the process disturbance and the measurement noise modeled as independent and identically distributed (i.i.d) zero-mean Gaus- sian random vectors N(0,Wn)andN(0,Vn), respectively. We assume that the spectral radius of An,n, is greaterthan one, which means that the dynamic processes are non- stationary,3making the remote estimation problem more inter- esting (see [25] and references therein). Due to the presence of noise in (1), each sensor nexecutes a classic Kalman filter to pre-process the raw measurement and generate state estimate xs n,tat each ti

=== greedy ===
for sensor n, say an, is randomly chosen from the setM(n)with probability , and is equal to a(n) n with probability 1. Ifa(n) n= 0, then the AoI-state threshold property cannot help with determining the action. Then, we define the action selected by the greedy policy (i.e., the conventional DQN method) at the state sas a(a1,a2, . . . , aN) = arg max aQ(s,a;). Thus, we set the SE action of sensor nidentical to the one generated by the conventional DQN method, i.e., an= an. Authorized licensed use limited to: TONGJI UNIVERSITY. Downloaded on February 03,2026 at 06:15:31 UTC from IEEE Xplore.  Restrictions apply.  388 IEEE TRANSACTIONS ON WIRELESS COMMUNICATIONS, VOL

=== The settings of the other hyperparameters ===
utation complexity of an FCNN isOPL1 l=1ulul+1 , where ulis the number of nodes in layer landLis the total number of layers of the NN [39]. Based on our computing platform, the inference time of the SE-DQN and SE-DDPG are 0.02 ms and 0.04 ms, respectively. The settings of the other hyperparameters for Algorithm 1 and Algorithm 2are summarized in Table I. Note that the input dimensions of the actor NN and the critic NN are different, as the inputs of the critic NN and the actor NN are the state vector and the state-action pair, respectively. During the training, we use the ADAM optimizer for calcu- lating the gradient and reset the environment for each episode with T= 500 steps. The episode numbers for the loo

=== three stages ===
od based on Definitions 1and2to select reasonable actions and hence enhance the data sampling efficiency; and 2) an SE loss function definition to add the penalty to sampled actions that do not follow the threshold structure. Our SE-DQN training algorithm has three stages: 1) the DQN with loose SE action selection stage, which only utilizes part of the structural property, 2) the DQN with tight SE action selection stage utilizes the full structural property, and 3) the conventional DQN stage. The first two stages use the SE action selection schemes and the SE loss function to train the DQN fast, resulting in a reasonable threshold policy, and the last stage is for further policy expl

