### **代码规范部分：**

用中文在每个函数前后补充参数说明，函数作用，返回结果，以及简要的函数核心步骤

### **系统建模部分**

**1. 物理动态系统与卡尔曼滤波 (Section II-A)**

*   **状态与观测方程 (Eq. 1):** 系统是LTI（线性时不变）系统。注意 $A_n$ 的谱半径（spectral radius）大于1，意味着系统是不稳定的，必须依赖调度来控制误差。
*   **【关键简化】稳态协方差 (Eq. 3 下方说明):** 论文假设本地卡尔曼滤波器已经收敛到稳态（Steady State），因此本地误差协方差矩阵 $P_{n,t}^s$ 简化为一个常数矩阵 $\bar{P}_n$。**在代码中，你不需要每个step都去跑卡尔曼滤波的五步公式，直接求解代数黎卡提方程（DARE）求出 $\bar{P}_n$ 即可。**

**2. 无线通信与丢包模型 (Section II-B)**
*   **信道分配约束 (Eq. 5):** N个传感器，M个信道。每个时刻只能选M个传感器，每个传感器最多分配1个信道。
*   **信道状态 (Eq. 4):** 信道是块衰落（block fading）的，被量化为 $\bar{h}$ 个离散状态。这构成了强化学习State的一部分。
*   **AoI (信息年龄) 演化 (Eq. 8):** 这是Env中最重要的状态转移公式。如果接收成功 $\eta_{n,t}=1$，AoI重置为1；否则 $\tau_{n,t} + 1$。
*   **【核心映射】AoI 到 奖励计算 (Eq. 9):** 远端估计误差 $P_{n,t}$ 完全由当前的AoI $\tau_{n,t}$ 决定：$P_{n,t} = f_n^{\tau_{n,t}}(\bar{P}_n)$。

**3. MDP 建模 (Section III-A) —— 确定 RL 的三要素**
* **State ($s_t$):** 由所有传感器的 AoI 向量 $\tau_t$ 和信道矩阵 $H_t$ 组成。

* **Action ($a_t$):** 从 $N$ 个传感器中选 $M$ 个分配给不同信道，动作空间大小为 $N! / (N-M)!$。

* **Reward ($r_t$):** 负的总体估计均方误差 (MSE) 之和，即 $-\sum_{n=1}^N \text{Tr}(P_{n,t})$。

  **4. 初始化参数 (Env Reset):**

      *   系统矩阵 $A_n$：你需要写一个函数，随机生成 $2 \times 2$ 的矩阵，并强制缩放使其最大特征值的绝对值（谱半径）处于 $[1, 1.4]$ 之间（这代表系统是不稳定的，MSE会随时间指数爆炸）。
          *   计算卡尔曼稳态协方差 $\bar{P}_n$：在环境初始化时，调用 `scipy.linalg.solve_discrete_are` 预先算好每个传感器的 $\bar{P}_n$，保存下来。

  *   **5. 步进逻辑 (Env Step):**
      *   输入动作 $a_t$（表示哪个传感器用了哪个信道）。
      *   根据瑞利分布（尺度参数 $0.5 \sim 2$）采样出信道状态，量化为5个等级（对应丢包率 `0.2, 0.15, 0.1, 0.05, 0.01`）。
      *   根据丢包率进行伯努利采样，决定这次传输是否成功（$\eta_{n,t} \in \{0,1\}$）。
      *   更新 AoI：成功的 AoI = 1，失败的 AoI = 当前 AoI + 1。
      *   计算 Reward：利用 $P_{n,t} = f_n^{\tau_{n,t}}(\bar{P}_n) = A_n^{\tau_{n,t}} \bar{P}_n (A_n^\top)^{\tau_{n,t}} + \sum_{k=0}^{\tau_{n,t}-1} A_n^k W_n (A_n^\top)^k$ 计算当前的协方差，Reward = $-\sum \text{Tr}(P_{n,t})$。

### 实验部分设置

**1. 环境参数设置 (Section VI-A)**

*   **矩阵维度:** 状态维度 $l_n = 2$，观测维度 $e_n = 1$。
*   **矩阵生成:** $A_n$ 是随机生成的，谱半径在 $(1, 1.4)$ 之间。$C_n$ 在 $(0, 1)$ 均匀分布，$W_n$ 和 $V_n$ 是单位阵。
*   **信道设置:** 量化等级 $\bar{h} = 5$。对应的丢包率 $\tilde{p}$ 为 `[0.2, 0.15, 0.1, 0.05, 0.01]`。信道状态的生成依赖于瑞利分布（尺度参数 0.5~2）。

**2. 强化学习网络与超参数 (Table I & VI-A)**
*   **网络结构:** 都是全连接神经网络(FCNN)。
    *   **SE-DQN:** 2层隐藏层，每层 256 个神经元。
    *   **SE-DDPG:** Actor和Critic各有 2层隐藏层，每层 512 个神经元。
*   **训练流程 (非常重要):** 论文提出了“三阶段”训练法：
    1.  **Loose SE 阶段:** 50 个 episode
    2.  **Tight SE 阶段:** 100 个 episode
    3.  **Conventional RL 阶段:** 150 个 episode (之后正常跑)
*   **关键超参数 (Table I):** $\gamma = 0.95$，Batch size = 128，SE-DQN的Loss权重 $\alpha_1 = 0.5$，SE-DDPG的Loss权重 $\alpha_2 = 0.9$。每个episode步数 $T = 500$。

DDPG动作映射逻辑：

**映射逻辑规划（你需要将其写成一个函数）：**
1.  **输入**：Actor 输出的连续向量 $\mathbf{v}$，例如 `v =[0.8, -0.2, 0.9, 0.1, -0.9, 0.5]`（长度为 $N=6$）。
2.  **排序取 Top-M**：获取这 $N$ 个值的降序索引。
    *   例如上面降序后前 3 名的索引是：`[2, 0, 5]`。
3.  **生成分配方案 Tuple**：
    *   排名第 1 的传感器（索引 2）分配给信道 1。
    *   排名第 2 的传感器（索引 0）分配给信道 2。
    *   排名第 3 的传感器（索引 5）分配给信道 3。
    *   其余未进前 3 的传感器分配信道 0。
    *   构成环境可读的 Tuple：`(2, 0, 1, 0, 0, 3)`。
4.  **查找对应 Action ID**：
    *   *(💡 性能优化建议)*：为了应对以后 $N=10, M=5$ 时庞大的 30240 动作空间，不要用 `list.index()` 去顺序查找。建议在初始化时构建一个哈希字典 `self.action_to_id_map = {action_tuple: id}`，实现 $O(1)$ 极速映射。
    *   将找出的整数 `action_id` 传给环境。
5.  这个逻辑根据项目需要写在对应文件中

### 文件设置：

Remote_Estimation_RL/
├── config.py    # 全局超参数与环境配置
├── core.py      # 核心数学引擎：系统矩阵、黎卡提方程、动作解码等
├── utils.py     # 辅助工具：训练曲线可视化、日志记录
├── env.py       # 强化学习环境 (MDP 状态机)
├── agent.py     # 强化学习智能体 (DQN/DDPG, 网络定义)
└── main.py      # 主程序入口